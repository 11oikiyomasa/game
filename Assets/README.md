# Assets

This directory is reserved for Unity project assets.

The first implementation slice keeps gameplay/domain logic separate from presentation. Unity scenes, prefabs, art, audio, and other presentation assets should be added only when the corresponding playable slice is implemented and verified.
